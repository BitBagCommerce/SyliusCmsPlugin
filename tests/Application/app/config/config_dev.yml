imports:
    - { resource: "config.yml" }

doctrine:
    dbal:
        path: "%kernel.root_dir%/../var/db_dev.sql"

framework:
    router:
        resource: "%kernel.root_dir%/config/routing_dev.yml"
