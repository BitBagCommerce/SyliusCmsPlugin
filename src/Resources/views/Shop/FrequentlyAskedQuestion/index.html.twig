{% extends '@SyliusShop/layout.html.twig' %}

{% import '@SyliusUi/Macro/messages.html.twig' as messages %}

{% block content %}
    <div class="ui stackable grid segment">
        <div class="ui sixteen wide column">
            {% if resources|length > 0 %}
                <div class="ui one column stackable grid" id="frequently-asked-questions">
                    {% for frequentlyAskedQuestion in resources %}
                        <div class="bitbag-frequently-asked-question-item">
                            {% include '@BitBagCmsPlugin/Shop/FrequentlyAskedQuestion/Index/_show.html.twig' %}
                        </div>
                    {% endfor %}
                </div>
            {% else %}
                {{ messages.info('sylius.ui.no_results_to_display') }}
            {% endif %}
        </div>
    </div>
{% endblock %}
