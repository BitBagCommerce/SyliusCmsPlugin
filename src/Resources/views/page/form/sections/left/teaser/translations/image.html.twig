{% set form = hookable_metadata.context.form %}
{% set locale = hookable_metadata.context.locale %}

<div class="col-12">
    {{ form_row(form.teaserImage, sylius_test_form_attribute('teaser-image', locale)) }}
    <a href="{{ path('sylius_cms_admin_media_create') }}" target="_blank" class="btn btn-primary" type="button">
        {{ 'sylius.ui.create'|trans }}
    </a>
</div>
